# Lombard & Piraux 2004 — ESIM 2D acoustic/elastic reproduction

**Paper anchor**: Lombard, B. & Piraux, J. (2004). "Numerical
treatment of two-dimensional interfaces for acoustic and elastic
waves." *Journal of Computational Physics* 195(1):90-116.
DOI [10.1016/j.jcp.2003.09.024](https://doi.org/10.1016/j.jcp.2003.09.024).
HAL: [hal-00004813](https://hal.science/hal-00004813).

**Local PDF**: `/tmp/lombard_piraux_2004.pdf` (downloaded via
Zotero web API; HAL hosts only the PDF, not LaTeX source).

## Role in the parent repo

This reproduction grounds the **path (e) Tier 3 ESIM
implementation** for Method 2 RSG in the TTI engagement
(`~/projects/reviews/`). Empirical bisection (plan
`~/.claude/plans/twinkling-popping-wadler.md` §"Final
architectural verdict") shows the Petrobras dipping-interface
artifact is generated by colocated-diagonal-stencil ×
discontinuous-interface coupling, NOT by stencil weights or
material averaging. Dual-reviewer (Codex + Gemini) consultation
identified ESIM-style jump-condition enforcement as the
principled fix. This folder reproduces the published algorithm
so the Tier 3 implementation can be verified against
byte-checkable invariants from the paper.

## What we reproduce

Per the `feedback_reproduction_quantitative_first` repo rule:
anchor on tabulated constants + scalar bounds + algorithmic
recipes, NOT on figure reproduction.

### 1. Fluid-solid jump conditions (paper Eq 10-11)

The order-0 jump matrices for the 2D fluid-elastic interface
along a parametric curve Γ(τ) with tangent t = (x', y') and
normal n = (-y', x'):

- **C₁⁰**: 2×3 matrix on fluid side (couples normal velocity
  v·n and pressure p)
- **C₂⁰**: 2×5 matrix on solid side (couples normal velocity
  v·n and normal stress σ·n)
- **L₁⁰**: 1×3 matrix (boundary conditions on fluid side —
  per paper Eq 11, empty for fluid-solid case)
- **L₂⁰**: 1×5 matrix (vanishing tangential shear stress on
  solid side)

Each matrix entry transcribed under **side-by-side user-
confirmed review** per the project-wide transcription protocol
(see `feedback_transcription_workflow_arxiv_then_side_by_side`
memory entry).

### 2. Compatibility conditions (paper Eq 21, 24)

Recursive differentiation produces compatibility constraints
between spatial derivatives of U:
- Eq 21: fluid vorticity = 0 (consequence of perfect fluid)
- Eq 24: solid stress-tensor symmetry

These reduce the number of independent components in U^k from
3(k+1)(k+2)/2 (fluid) or 5(k+1)(k+2)/2 (solid) to a smaller
set via the V^k = G^k V^k transformation.

### 3. SVD-derived nullspace K (paper Eq 26)

K^k_i is the kernel of L^k_i G^k_i, computed via SVD. Encodes
which independent components of U^k can be chosen freely
without violating the boundary condition.

### 4. Modified-value projector (paper Eq 42)

The ESIM modified value at irregular point M(I, J) ∈ Ω₂:
```
U*_{I,J} = Π^k_{I,J} · G^k_2 · K^k_2 · M̄⁻¹ · (U^n)_B
```
where:
- Π^k_{I,J}: Taylor-basis at (I, J) (Eq 32)
- M̄⁻¹: least-squares restriction inverse of the Taylor-fit
  matrix M (Eq 38-40)
- (U^n)_B: numerical values at neighborhood disk B around the
  orthogonal projection P of M on Γ

### 5. Time-stepping near interfaces (paper Eq 43-44)

At irregular point (i, j) belonging to medium Ω(i, j):
- Stencil reads from cells on the SAME side: use numerical
  values U^n
- Stencil reads from cells on the OTHER side: use modified
  values U*
- Apply the standard scheme H to the mixed Ũ array

## Transcription provenance

All entries verified under the user-mandated arXiv-first /
side-by-side review protocol
(`feedback_transcription_workflow_arxiv_then_side_by_side`).
HAL deposit has PDF only (no LaTeX source), so every entry below
went through side-by-side review with pdflatex-rendered
transcription on the right and `pdftocairo`-extracted paper region
on the left.

| Entry | Source | Status |
|---|---|---|
| Eq 10-11 jump-condition matrices C₁⁰, C₂⁰, L₁⁰, L₂⁰ | paper PDF p. 8 | ✓ user-confirmed (side-by-side v3) |
| Eq 20-24 compatibility (α₁, α₂; fluid vorticity; solid σ-symmetry) | paper PDF p. 11 | ✓ user-confirmed (side-by-side v2; α₁/α₂ bracket fix) |
| Eq 26-31 interface coupling (K, S, R_S₂) | paper PDF p. 12 | ✓ user-confirmed (side-by-side v1) |
| Eq 32-37 ESIM Taylor setup (Π, Taylor expansions B₁/B₂) | paper PDF pp. 13-14 | ✓ user-confirmed (stitched side-by-side v1) |
| Eq 38-42 master ESIM (M̄⁻¹, U*_{I,J}) | paper PDF p. 15 | ✓ user-confirmed (side-by-side v1) |
| Eq 43-44 time-stepping at irregular points | paper PDF p. 16 | ✓ user-confirmed (side-by-side v1) |
| Appendix A.1, A.2 G_i^k construction (fluid + solid pseudocode) | paper PDF pp. 30-31 | ✓ user-confirmed (side-by-side v1) |
| Eq A.3-A.7 SVD pseudoinverse | paper PDF p. 31 | textbook (numpy.linalg.svd / lstsq; no transcription risk) |

All side-by-side review PDFs preserved at
`transcription_review/lp04_*.tex` (transcription LaTeX) +
re-generable via `transcription_review/side_by_side.py`.

## Quick start

```bash
cd reproduced/doi_10.1016_j.jcp.2003.09.024/
uv sync
uv run pytest tests/ -v
```

## File layout

```
doi_10.1016_j.jcp.2003.09.024/
├── pyproject.toml
├── README.md                          # this file
├── paper_tables.py                    # byte-transcribed constants
├── run_reproduction.py                # builds M̄⁻¹ for k=2, verifies
│                                      # the jump conditions to fp64
├── tests/
│   └── test_paper_tables.py
└── reference_outputs/
    └── (pinned modified-value matrices for specific configs)
```

## Status

🟢 PAPER-TABLES TRANSCRIPTION COMPLETE — all load-bearing
equations (10-11, 20-24, 26-31, 32-37, 38-42, 43-44) and
Appendix A.1-A.2 (G_i^k pseudocode) byte-transcribed under
user-confirmed side-by-side review.

🟢 LP04 §3.1 RECURSIVE C_i^k / L_i^k PORT (2026-05-27) — the
recursive jump-condition matrices via Eq 13-18 are now
implemented in `esim_recursion.py`. This replaces the prior
block-diagonal `C_i^k`/`L_i^k` construction (which was a
non-equivalent simplification flagged by the prior Codex
DISAGREE; see `transcription_review/lp04_close_out_codex.txt`
+ `lp04_R_plan_review.txt`).

Empirical impact on projector-vs-analytical convergence
(`tests/test_projector_vs_analytical.py`):

| Target side | Pre-port slope (block-diagonal) | Post-port slope (recursive) |
|---|---|---|
| solid     | 1.81                            | **2.71** |
| fluid     | 1.81                            | **1.96** |

49 / 49 pytest tests pass post-port (including 4 projector-vs-
analytical correspondence + 6 R/T BC continuity + 5 Lax-Wendroff
bulk convergence + 14 paper-table byte-match + 8 projector
structural + 5 R/T oblique + 5 LW homog + 2 misc).

The LP04-R port also closed Codex's plan-review F1 finding
(monomial-order mismatch) by using the reproduction's
paper/x-first `minimal_multi_indices` ordering, and F2 (sign
convention) via `flux_jacobians_fluid` documented to use
LP04's extensional-positive p = -p_physical convention
matching `paper_tables.C1_zero` + `esim_apply.py:159-180`.

🟡 INTEGRATED CONVERGENCE — PARTIAL — the
`run_reproduction_interface.py` plane-interface sweep over
Nx ∈ {60, 100, 150, 200, 300} gives:

| Norm | Pre-port order | Post-port order | Target |
|---|---|---|---|
| L^∞ | 0.73 (FAIL ≥1.5) | 0.93 (FAIL ≥1.5) | ≥1.8 |
| L^1 | 1.52 (PASS ≥1.5) | **1.50** (PASS) | ≥1.8 |

**Diagnostic finding (2026-05-27 extended sweep + LW-alone baseline):**

| Approach | L^∞ slope | L^∞ at Nx=200 |
|---|---|---|
| LW-alone, heterogeneous material (no ESIM) | 1.01 | 4.2e5 |
| LP04 (recursive C/L + per-side LW + scatter)  | 0.93 | 1.5e5 |

LP04 ESIM substantially improves the ABSOLUTE L^∞ magnitude
(~2-3× smaller error at each grid) but does NOT fully restore
order 2 at these resolutions. Both approaches are limited by
the discrete material discontinuity — a known LW pre-asymptotic
regime that LP04 §3 is designed to fix in principle. At Nx=300
the L^∞ peak sits 8 cells from Γ (NOT at irregular cells),
indicating bulk error propagation from the discontinuity rather
than a localised projector defect.

The remaining gap to LP04 §4.2 Table 2's claimed L^∞ order ~2.0
likely requires (a) strict per-cell same-side/other-side leg
mixing per LP04 Eq 43-44 (the current per-side LW + scatter is
an approximation), AND/OR (b) finer grids (Nx ≥ 400) where the
asymptotic regime sets in, AND/OR (c) higher Taylor order k.
Estimated 2-3 days additional engineering.

🟡 LP04 §4.2 OBLIQUE GEOMETRY DEFERRED — the user-authorised
horizontal + oblique scope is partially met: horizontal is
in place, oblique (80°-inclined Γ + 21° incidence) requires
extending `find_irregular_cells_horizontal` →
`find_irregular_cells_oblique` + extending
`PlaneWaveAcousticElastic` for oblique R/T. Estimated 1-2
days; deferred until the LW × U_tilde integration glue lands
a fix that brings L^∞ order ≥ 1.5 on horizontal.

## Provenance classification

**`novel-combination`** (post-2026-05-27 port). The components:

- **LP04 §3.1 recursive C_i^k / L_i^k construction** —
  paper-faithful per Eq 13-18, verified by the
  projector-vs-analytical convergence slope 1.96-2.71.
- **LP04 §3 jump conditions + compatibility matrices**
  (paper_tables.py Eq 10-11, 20-24, 26-31, A.1, A.2) — paper-
  faithful per side-by-side user-confirmed transcription.
- **Lax-Wendroff bulk** — Strang-Toro-Dumbser LW; LP04
  itself uses LW so this matches the paper's bulk choice.
- **LW × U_tilde integration glue** — currently per-side
  homogeneous LW pass with U* scattered at irregular cells
  (LP04 Eq 43-44 simplified). The full LP04 Eq 43-44 calls
  for the U^n at SAME-side stencil legs + U* at OTHER-side
  legs — this distinction may be the missing piece for
  L^∞ order ≥ 1.8. Investigation pending.

The classification is `novel-combination` not `published`
because the L^∞ integrated convergence is below 1.5; the
projector itself is paper-faithful but the integration glue
needs refinement before claiming full LP04 §4.2 Table 2
reproduction.

Tier 3 implementation in Method 2 RSG will consume this
transcription via `paper_tables.build_G_fluid(k)`,
`build_G_solid(k, α₁, α₂)`, `alpha_coefficients(c_p, c_s)`,
plus the matrix definitions C₁⁰, C₂⁰, L₁⁰, L₂⁰ as the
SVD-input matrices for K_i^k and S_i^k.
