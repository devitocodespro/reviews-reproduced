# Reference outputs

The byte-checkable load-bearing artifacts for this reproduction
live in `../paper_tables.py` (Tables 1 + 2 from the paper PDF
page 4, transcribed byte-identically) and are exercised by
`../tests/test_paper_tables.py` (11 tests, all PASS).

Per the user's standing rule
(`feedback_reproduction_quantitative_first.md`), the
reproduction anchors on the paper's published tables and scalar
bounds — NOT visual figure reproduction. Figures from the
5-stage verification pipeline (paper Fig 1 / Fig 2 / Fig 3
reproductions + stage-specific outputs) live in `../figures/`
and serve as supporting evidence.
